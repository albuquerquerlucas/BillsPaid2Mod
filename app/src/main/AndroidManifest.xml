<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.samuniz.billspaid2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activitys.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activitys.CadastroActivity" />
        <activity android:name=".Activitys.MainActivity" />
        <activity android:name=".Activitys.ContaActivity" />
        <activity android:name=".Activitys.ContasActivity" />
        <activity android:name=".Activitys.DespesasActivity" />
        <activity android:name=".Activitys.ReceitasActivity"></activity>
    </application>

</manifest>